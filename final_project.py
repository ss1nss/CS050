# I want to make a calorie tracker turned into a game, pretty much a false calorie tracker turned into like stanley parable or those indie games lol

select_difficulty()

select_difficulty() to boss_health()

foods["calories"]["ingredients"]

damage = foods["calories"] * .01

if damage > boss_health():
    boss_health() * 10

try:
    attack()
    if not_dead():
        attack_run_defend (# can print out cowsay to give options lol, randomly switching between different animals)
    else:
        print("Victory")
        break
